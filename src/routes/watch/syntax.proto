






// read currently watching
db.user.data.read(data => {
    let currently_watching = data.currently_watching

    alert("you are watching " + currently_watching)
})

// update currently watching
db.user.data.write('currently_watching', 'The Office')


db.read(user => {
    let data = JSON.parse()
})